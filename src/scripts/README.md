# Scripts

One-time or maintenance utility scripts.

Examples:
- `migrate-news-slugs.ts` — Backfill missing slugs
- `cleanup-deleted-files.ts` — Permanently remove soft-deleted files
- `seed-categories.ts` — Seed default news categories
